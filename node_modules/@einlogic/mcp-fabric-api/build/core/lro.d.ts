import { FabricClient } from "../client/fabric-client.js";
import type { OperationState } from "./types.js";
export interface LroOptions {
    pollIntervalMs?: number;
    timeoutMs?: number;
}
export declare function pollOperation(client: FabricClient, operationId: string, options?: LroOptions): Promise<OperationState>;
export declare function getOperationResult<T>(client: FabricClient, operationId: string): Promise<T | undefined>;
//# sourceMappingURL=lro.d.ts.map