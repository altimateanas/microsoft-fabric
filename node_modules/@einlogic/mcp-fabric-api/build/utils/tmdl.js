export function decodeTmdlParts(parts) {
    return parts.map((part) => ({
        path: part.path,
        content: part.payloadType === "InlineBase64"
            ? Buffer.from(part.payload, "base64").toString("utf-8")
            : part.payload,
    }));
}
export function encodeTmdlParts(files) {
    return files.map((file) => ({
        path: file.path,
        payload: Buffer.from(file.content, "utf-8").toString("base64"),
        payloadType: "InlineBase64",
    }));
}
export function formatTmdlOutput(files) {
    return files
        .map((file) => `--- ${file.path} ---\n${file.content}`)
        .join("\n\n");
}
//# sourceMappingURL=tmdl.js.map