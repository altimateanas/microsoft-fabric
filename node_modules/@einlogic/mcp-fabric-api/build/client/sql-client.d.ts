import { TokenManager } from "../auth/token-manager.js";
import type { SqlQueryResult } from "../core/types.js";
export declare class SqlClient {
    private tokenManager;
    constructor(tokenManager: TokenManager);
    executeQuery(server: string, database: string, query: string, maxRows?: number): Promise<SqlQueryResult>;
}
//# sourceMappingURL=sql-client.d.ts.map