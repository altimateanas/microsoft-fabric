import { TokenManager } from "../auth/token-manager.js";
import type { KqlQueryResult } from "../core/types.js";
export declare class KustoClient {
    private tokenManager;
    constructor(tokenManager: TokenManager);
    executeQuery(queryServiceUri: string, database: string, kql: string, maxRows?: number): Promise<KqlQueryResult>;
}
//# sourceMappingURL=kusto-client.d.ts.map